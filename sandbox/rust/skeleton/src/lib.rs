/// Placeholder used to warm the sandbox compilation cache at image build time.
/// The learner's submission replaces this file in each run's Sandbox Workspace.
pub fn warmup() -> &'static str {
    "warm"
}
