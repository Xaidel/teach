// Package exercise holds the learner's submission at run time. This
// placeholder is used to warm the sandbox build cache at image build time.
package exercise

// Warmup returns a fixed value so the skeleton compiles and tests.
func Warmup() string {
	return "warm"
}
